This is not necessarily an exhaustive list of questions, but they are definitely good to know! You can also look at Rev Pro for the different topics as well as our notes repo.

## Java
- Explain the JDK, JRE, JVM?
- What is meant by Write Once, Run Anywhere?
- What is a package in Java?
- What are the primitive types in Java?
- What are the non-access modifiers in Java?
- What are the access modifiers in Java?
- What is the difference between static and final variables?
- What are the four pillars of OOP?
- How do we achieve abstraction in Java?
- What is the difference between an abstract class and an interface in Java?
- What is the difference between == and .equals() in Java?
- What is polymorphism?
- What is inheritance?
- What is the difference between overloading and overriding?
- What are the different variable scopes in Java?
- What is the difference between an error and an exception?
- What is the difference between a checked and an unchecked exception?
- How can we handle exceptions in Java?
- What is a wrapper class? How is a wrapper class useful?
- What is the difference between the stack and heap?
- What is garbage collection?
- What is casting in Java?
- What is the Object class? What are some of its methods?
- What is a constructor? How are they written in Java?
- Explain the Collections Hierarchy.
- Compare and Contrast between Lists, Sets, and Queues.
- What are Maps? How are they different from Lists, Sets, and Queues?
- What is the difference between Comparable and Comparator?
- What are Generics?
- Explain the String pool in Java.
- What are StringBuilder and StringBuffer?

## Java 8
- What is a lambda expression? What is the syntax like?
- Explain the method reference syntax and how it is used?
- What is the Optional class used for?
- What is a Functional Interface?
- What are some of the built-in Functional Interfaces in Java?

## SQL
- What is a relational database?
- How are relational databases structured?
- What is a schema?
- What is a primary key?
- What is a foreign key?
- What are the different types of multiplicity relationships?
- What is referential integrity?
- What are the different data types in SQL?
- What are the 5 sub-languages of SQL?
- Which commands fall under each sub-language of SQL?
- What is the difference between drop, delete, and truncate?
- What are some different clauses we can use with DQL?
- What are the different set operations in SQL?
- What is a sub-query?
- What is a view?
- What is a transaction?
- What are the different types of joins?
- What is the difference between a right and left join?
- What is the difference between an inner and outer join?
- What is a cross-join? 
- What are equi and theta joins?
- What is the difference between scalar and aggregate functions?
- What are some examples of scalar functions?
- What are some examples of aggregate functions?
- What is an index?
- Why are indexes useful?
- What is a stored procedure? 
- What is a function? How are they different from stored procedures?

## JDBC
- What is Maven?
- What is the purpose of the POM file?
- What is JDBC?
- What is the DAO design pettern?
- What is SQL injection?
- How can we prevent SQL injection?
- What is Logback?
- What are the different logging levels?
- Explain how a jdbc app works.
- What is the difference between where and having?

## Spring
- [Spring questions here](./Spring.md)


## REST/HTTP
- What does REST stand for?
- Describe the different REST constraints.
- Why is it unadvised to use verbs in your endpoints?
- What does HTTP stand for?
- What is an HTTP request?
- What is an HTTP response?
- What is meant by idempotent? Which HTTP Methods are idempotent?
- Describe and give use cases for the following HTTP Methods
  - GET
  - PUT
  - POST
  - DELETE
  - PATCH
- What do status codes in the 100s mean? (error/success/client-side/server-side)
  - 200s
  - 300s
  - 400s
  - 500s
- What do the following status codes mean specifically?
  - 200
  - 201
  - 400
  - 404