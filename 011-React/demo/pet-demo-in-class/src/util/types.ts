// define the types for our entities:
export interface Pet {
  id?: number,
  name: string,
  species: string,
  food: string
}

export interface Person {
  id?: number,
  name: string,
  password: string
}